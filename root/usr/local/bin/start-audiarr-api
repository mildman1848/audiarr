#!/usr/bin/with-contenv bash
# shellcheck shell=bash
set -euo pipefail
export HOME=/config
export AUDIARR_CONFIG_DIR=/config
exec s6-setuidgid abc /app/venv/bin/uvicorn app.main:app \
  --host "${AUDIARR_HOST:-0.0.0.0}" \
  --port "${AUDIARR_PORT:-8787}" \
  --app-dir /app/audiarr
